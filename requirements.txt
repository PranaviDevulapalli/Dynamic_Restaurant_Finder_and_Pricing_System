Django>=4.0,<5.0
gunicorn>=20.1.0  # For production server (optional, based on your server choice)
psycopg2>=2.9.3   # For PostgreSQL database (use 'mysqlclient' or 'sqlite' for different databases)
Pillow>=8.3.0     # For image processing (optional, if your project handles images)
djangorestframework>=3.12.0  # If you're using Django REST Framework (optional)
